# Socket Web Client

Test client for connecting to a NestJS backend using Socket.IO.

## Requirements

- Node.js 18 or higher
- pnpm
- A NestJS backend running and exposing Socket.IO

## Installation

```bash
pnpm install